[tool.mypy]
exclude = ".venv/"

[tool.black]
line-length = 79

[tool.isort]
line_length = 79
profile = "black"

[tool.flake8]
exclude = [
    "venv",
    ".venv",
    "__pycache__",
    ".git",
    ".mypy_cache",
    ".pytest_cache",
    ".vscode",
]
max-line-length = 79
